# Prompt 20 — StatusActions Component Tests

## Prompt
> Now write React Testing Library component tests for the ticket status form and its validation logic. Cover: valid submission, invalid state transition, and required-field errors. Show me one test file first before generating the rest.

## Delivered
- [`frontend/components/tickets/StatusActions.test.tsx`](../frontend/components/tickets/StatusActions.test.tsx)
- [`frontend/vitest.config.ts`](../frontend/vitest.config.ts), `vitest.setup.ts`, test scripts in `package.json`
- Tests: client-side transition buttons, valid API submit, 409 error alert, validation error alert

## Status
**Waiting for user confirmation** before additional frontend test files (e.g. CreateTicketForm).
