# Prompt 23 — Migrations & Seed Workflow

## Prompt
> Add a MIGRATIONS.md documenting my Prisma migration workflow: how to run migrations, how the seed script works, and the steps to reset the DB locally. Base it on my actual prisma/ folder and package.json scripts.

## Delivered
- [`MIGRATIONS.md`](../MIGRATIONS.md) — scripts table, migration history, seed vs `db:seed:demo`, local reset options
- Grounded in `backend/prisma/`, `package.json`, `vercel.json` build command
