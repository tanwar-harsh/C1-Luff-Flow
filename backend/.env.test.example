# Test environment — use a separate DB/branch when possible
# If localhost, tests fall back to DATABASE_URL from .env (see src/tests/setup.ts)
PORT=3001
NODE_ENV=test
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/support_tickets_test?schema=public"
CORS_ORIGIN=http://localhost:3000
